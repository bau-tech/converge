fastapi>=0.116.0
uvicorn[standard]
pydantic>=2.0
psycopg2-binary
# Pinned to a pre-release: bundle-object read support (operations.receive
# dispatching on bundle references) landed only in 2026.9.0a1+, not yet in
# any stable release. Needed to ingest commits from servers already storing
# data in the newer bundle format (e.g. app.speckle.systems on 2026.9.3).
specklepy==2026.9.0b3
# specklepy's bundle-format reader (specklepy.bundle.envelope_writer, used
# by operations.receive's bundle dispatch above) imports pyarrow directly
# but doesn't declare it as an install dependency.
pyarrow
ifcopenshell>=0.7.0
ifc5d>=0.8.0
ifctester>=0.8.0
ifcclash>=0.8.0
requests
httpx>=0.28.1
python-dotenv
mcp[cli]>=1.0.0,<2.0.0
pyjwt>=2.8.0
python-multipart
bcrypt>=4.0.0
numpy
fastembed>=0.3.0
ezdxf>=1.4.0
cairosvg>=2.9.0
python-docx>=1.1.0
openpyxl>=3.1.0
reportlab>=4.2.0
